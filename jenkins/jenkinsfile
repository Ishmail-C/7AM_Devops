pipeline{
    agent any

    stages{
        stage('ip_validation'){
            steps{
                sh '''
                uname -a

                free -h

                df -h
		
		'echo " Git intigrated with Jenkins"'

                '''
            }
        }
    }   
}
