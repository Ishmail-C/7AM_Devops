pipeline {
    agent any

    parameters {
            string (defaultValue: "None", description: 'Which environment to deploy in:', name: 'deployEnv')
    }

    stages {
        stage("Input validation") {
            steps {
                echo "String set to: ${params.deployEnv}"
            }
        }
    }
}
