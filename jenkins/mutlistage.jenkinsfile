pipeline {
    agent any
    stages {
        stage('node') {
            agent {
                label 'node1'
            }
            steps {
                sh '''
                echo "Node IP:`hostname -I`"
                echo " this is slave "
                '''
            }
        }
        stage('master') {
            steps {
                sh 'echo "Master IP:`hostname -I`"'
                
            }
        }
    }
}
