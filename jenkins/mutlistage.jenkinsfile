pipeline {
    agent any
    options {
        skipDefaultCheckout(true)
    }
    stages {
        stage('Build') {
            steps {
            sh '''
                echo " `hostname` " 
            '''
            }
        }
        stage("Test") {
            agent { label 'node1' }
            steps {
                sh '''
                   echo " hostname : `hostname` "
                '''
            }
        }
    }
}
